# Copy to .env.local for local canonical URLs and sitemap generation.
# On Vercel, set VITE_SITE_URL to your production domain (e.g. https://choros.example.com).
# If unset on Vercel, VERCEL_URL is used automatically during builds.

VITE_SITE_URL=
